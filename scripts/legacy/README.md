Legacy scripts are kept here for reference only.

These scripts do not match the current production deployment model
(systemd + single Docker container). Use them only if you intentionally
switch back to a docker-compose based deploy.
